node_modules/
.DS_Store
*.log
.npmrc

.mcpregistry_*
.launch-day/

# MCP Registry publisher binary (downloaded locally, not for commit)
mcp-publisher

# Smithery build artifacts
.smithery/

# Local backups / debug artifacts (never commit)
*.bak
*.bak-before-*
.archive/
audit-*.md
.playwright-cli/

# כלי עזר מקומי (TCC) — לא חלק מהחבילה
claude-tcc-fixer.swift

# תוצרי ביניים של צנרת התוכן (טיוטות לייבוא, תמונות הירו) — נשארים מקומית בלבד
devto-draft-*.md
meta-agent-il-*.html
hero-*.png
